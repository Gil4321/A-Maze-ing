[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Maze generator package"
authors = [{name="adghouai"}, {name="theomart"}]
requires-python = ">=3.13"